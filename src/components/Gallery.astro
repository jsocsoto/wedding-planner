---
import type { translations } from '../i18n/translations';

interface Props {
  t: typeof translations.es | typeof translations.en;
}

const { t } = Astro.props;

// Imágenes de bodas de Unsplash (gratis y de alta calidad)
const galleryImages = [
  { 
    id: 1, 
    url: 'https://images.unsplash.com/photo-1519741497674-611481863552?w=1200&q=80',
    alt: 'Ceremonia de boda elegante'
  },
  { 
    id: 2, 
    url: 'https://images.unsplash.com/photo-1606800052052-a08af7148866?w=1200&q=80',
    alt: 'Recepción de boda decorada'
  },
  { 
    id: 3, 
    url: 'https://images.unsplash.com/photo-1465495976277-4387d4b0b4c6?w=1200&q=80',
    alt: 'Detalles de decoración floral'
  },
  { 
    id: 4, 
    url: 'https://images.unsplash.com/photo-1583939003579-730e3918a45a?w=1200&q=80',
    alt: 'Pareja de novios'
  },
  { 
    id: 5, 
    url: 'https://images.unsplash.com/photo-1511285560929-80b456fea0bc?w=1200&q=80',
    alt: 'Mesa de recepción elegante'
  },
  { 
    id: 6, 
    url: 'https://images.unsplash.com/photo-1520854221256-17451cc331bf?w=1200&q=80',
    alt: 'Venue de boda al aire libre'
  },
  { 
    id: 7, 
    url: 'https://images.unsplash.com/photo-1591604466107-ec97de577aff?w=1200&q=80',
    alt: 'Anillos de boda'
  },
  { 
    id: 8, 
    url: 'https://images.unsplash.com/photo-1522673607200-164d1b6ce486?w=1200&q=80',
    alt: 'Bouquet de novia'
  }
];
---

<section id="gallery" class="gallery">
  <div class="container">
    <h2 class="section-title">{t.gallery.title}</h2>
    <p class="section-subtitle">{t.gallery.subtitle}</p>
    
    <div id="weddingCarousel" class="carousel slide carousel-fade" data-bs-ride="carousel">
      <div class="carousel-inner">
        {galleryImages.map((image, index) => (
          <div class={`carousel-item ${index === 0 ? 'active' : ''}`} data-bs-interval="4000">
            <img src={image.url} class="d-block w-100" alt={image.alt} loading="lazy" />
            <div class="carousel-overlay"></div>
            <div class="carousel-caption">
              <h3>{image.alt}</h3>
            </div>
          </div>
        ))}
      </div>
      
      <button class="carousel-control-prev" type="button" data-bs-target="#weddingCarousel" data-bs-slide="prev">
        <span class="carousel-control-prev-icon" aria-hidden="true"></span>
        <span class="visually-hidden">Anterior</span>
      </button>
      <button class="carousel-control-next" type="button" data-bs-target="#weddingCarousel" data-bs-slide="next">
        <span class="carousel-control-next-icon" aria-hidden="true"></span>
        <span class="visually-hidden">Siguiente</span>
      </button>

      <!-- Thumbnails modernos flotantes -->
      <div class="carousel-thumbnails">
        {galleryImages.map((image, index) => (
          <div 
            class={`thumb-item ${index === 0 ? 'active' : ''}`}
            data-bs-target="#weddingCarousel" 
            data-bs-slide-to={index}
            role="button"
            tabindex="0">
            <img src={image.url} alt={image.alt} loading="lazy" />
            <div class="thumb-overlay"></div>
          </div>
        ))}
      </div>
    </div>
  </div>
</section>

<style>
  .gallery {
    padding: 6rem 2rem;
    background: var(--bg-light);
  }

  .container {
    max-width: 1400px;
    margin: 0 auto;
  }

  .section-title {
    font-family: 'Cormorant Garamond', serif;
    font-size: clamp(2rem, 4vw, 3rem);
    color: var(--primary);
    text-align: center;
    margin-bottom: 1rem;
  }

  .section-subtitle {
    text-align: center;
    color: var(--text-secondary);
    font-size: 1.1rem;
    margin-bottom: 4rem;
  }

  .carousel {
    position: relative;
    border-radius: 0;
    overflow: visible;
  }

  .carousel-fade .carousel-item {
    opacity: 0;
    transition: opacity 0.8s ease-in-out;
  }

  .carousel-fade .carousel-item.active {
    opacity: 1;
  }

  .carousel-inner {
    border-radius: 0;
    box-shadow: 0 10px 40px rgba(0, 0, 0, 0.15);
    overflow: hidden;
  }

  .carousel-item {
    height: 70vh;
    min-height: 500px;
    position: relative;
  }

  .carousel-item img {
    height: 100%;
    object-fit: cover;
    object-position: center;
    filter: brightness(0.95);
  }

  .carousel-overlay {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: linear-gradient(to bottom, rgba(0,0,0,0) 50%, rgba(0,0,0,0.4) 100%);
    pointer-events: none;
  }

  .carousel-caption {
    position: absolute;
    bottom: 3rem;
    left: 3rem;
    right: auto;
    text-align: left;
    background: rgba(255, 255, 255, 0.95);
    padding: 1.5rem 2.5rem;
    border-left: 4px solid var(--primary);
    backdrop-filter: blur(10px);
    max-width: 500px;
  }

  .carousel-caption h3 {
    font-family: 'Cormorant Garamond', serif;
    font-size: 1.8rem;
    margin: 0;
    color: var(--text-primary);
    font-weight: 400;
    letter-spacing: 0.5px;
  }

  .carousel-control-prev,
  .carousel-control-next {
    width: 60px;
    height: 60px;
    top: 50%;
    transform: translateY(-50%);
    opacity: 1;
    background: rgba(255, 255, 255, 0.9);
    border-radius: 50%;
    transition: all 0.3s ease;
  }

  .carousel-control-prev {
    left: 2rem;
  }

  .carousel-control-next {
    right: 2rem;
  }

  .carousel-control-prev:hover,
  .carousel-control-next:hover {
    background: white;
    transform: translateY(-50%) scale(1.1);
  }

  .carousel-control-prev-icon,
  .carousel-control-next-icon {
    background-size: 50%;
    filter: invert(1);
    opacity: 0.8;
  }

  /* Thumbnails modernos flotantes */
  .carousel-thumbnails {
    position: absolute;
    bottom: 2rem;
    right: 2rem;
    display: flex;
    gap: 0.75rem;
    z-index: 10;
    background: rgba(255, 255, 255, 0.95);
    padding: 1rem;
    border-radius: 50px;
    backdrop-filter: blur(10px);
    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.15);
  }

  .thumb-item {
    width: 60px;
    height: 60px;
    border-radius: 50%;
    overflow: hidden;
    cursor: pointer;
    transition: all 0.3s ease;
    position: relative;
    border: 3px solid transparent;
  }

  .thumb-item.active {
    border-color: var(--primary);
    transform: scale(1.15);
  }

  .thumb-item:hover {
    transform: scale(1.1);
  }

  .thumb-item img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
  }

  .thumb-overlay {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: rgba(0, 0, 0, 0.3);
    opacity: 1;
    transition: opacity 0.3s ease;
  }

  .thumb-item.active .thumb-overlay,
  .thumb-item:hover .thumb-overlay {
    opacity: 0;
  }

  @media (max-width: 968px) {
    .gallery {
      padding: 3rem 1rem;
    }

    .section-title {
      font-size: 2rem;
    }

    .section-subtitle {
      font-size: 1rem;
      margin-bottom: 2.5rem;
    }

    .carousel-item {
      height: 50vh;
      min-height: 400px;
    }

    .carousel-caption {
      bottom: 1.5rem;
      left: 1.5rem;
      padding: 1rem 1.5rem;
      max-width: calc(100% - 3rem);
    }

    .carousel-caption h3 {
      font-size: 1.3rem;
    }

    .carousel-control-prev,
    .carousel-control-next {
      width: 45px;
      height: 45px;
    }

    .carousel-control-prev {
      left: 1rem;
    }

    .carousel-control-next {
      right: 1rem;
    }

    .carousel-thumbnails {
      bottom: auto;
      top: 1rem;
      right: 1rem;
      left: auto;
      flex-direction: column;
      padding: 0.5rem;
      border-radius: 30px;
    }

    .thumb-item {
      width: 45px;
      height: 45px;
    }
  }
</style>

<script>
  // Sincronizar miniaturas con el carrusel
  const carousel = document.querySelector('#weddingCarousel');
  if (carousel) {
    carousel.addEventListener('slide.bs.carousel', (e: any) => {
      // Remover active de todas las miniaturas
      document.querySelectorAll('.thumb-item').forEach(thumb => {
        thumb.classList.remove('active');
      });
      // Agregar active a la miniatura correspondiente
      const activeThumb = document.querySelector(`.thumb-item[data-bs-slide-to="${e.to}"]`);
      if (activeThumb) {
        activeThumb.classList.add('active');
      }
    });
  }
</script>
