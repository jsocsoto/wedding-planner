---
import type { translations } from '../i18n/translations';

interface Props {
  t: typeof translations.es | typeof translations.en;
}

const { t } = Astro.props;

// Placeholder images - se pueden reemplazar con imágenes reales
const galleryImages = [
  { id: 1, alt: 'Wedding ceremony' },
  { id: 2, alt: 'Wedding reception' },
  { id: 3, alt: 'Wedding decoration' },
  { id: 4, alt: 'Wedding couple' },
  { id: 5, alt: 'Wedding details' },
  { id: 6, alt: 'Wedding venue' }
];
---

<section id="gallery" class="gallery">
  <div class="container">
    <h2 class="section-title">{t.gallery.title}</h2>
    <p class="section-subtitle">{t.gallery.subtitle}</p>
    
    <div class="gallery-grid">
      {galleryImages.map((image) => (
        <div class="gallery-item">
          <div class="image-placeholder">
            <span class="placeholder-icon">📸</span>
            <p class="placeholder-text">{image.alt}</p>
          </div>
        </div>
      ))}
    </div>
  </div>
</section>

<style>
  .gallery {
    padding: 6rem 2rem;
    background: linear-gradient(180deg, var(--bg-light) 0%, var(--white) 100%);
  }

  .container {
    max-width: 1200px;
    margin: 0 auto;
  }

  .section-title {
    font-family: 'Playfair Display', serif;
    font-size: clamp(2rem, 4vw, 3rem);
    color: var(--primary);
    text-align: center;
    margin-bottom: 1rem;
  }

  .section-subtitle {
    text-align: center;
    color: var(--text-secondary);
    font-size: 1.1rem;
    margin-bottom: 4rem;
  }

  .gallery-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 1.5rem;
  }

  .gallery-item {
    position: relative;
    overflow: hidden;
    border-radius: 10px;
    aspect-ratio: 4/3;
    cursor: pointer;
    transition: transform 0.3s ease;
  }

  .gallery-item:hover {
    transform: scale(1.05);
  }

  .image-placeholder {
    width: 100%;
    height: 100%;
    background: linear-gradient(135deg, #ffe8f0 0%, #f7d5e3 100%);
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    transition: all 0.3s ease;
  }

  .gallery-item:hover .image-placeholder {
    background: linear-gradient(135deg, #f7d5e3 0%, #e8b4c8 100%);
  }

  .placeholder-icon {
    font-size: 3rem;
    margin-bottom: 1rem;
    opacity: 0.7;
  }

  .placeholder-text {
    color: var(--text-secondary);
    font-size: 0.9rem;
    text-align: center;
    opacity: 0.7;
  }

  @media (max-width: 768px) {
    .gallery {
      padding: 4rem 1.5rem;
    }

    .gallery-grid {
      grid-template-columns: 1fr;
    }
  }
</style>

<script>
  // Modal para ver imágenes en grande (se puede implementar más adelante)
  document.querySelectorAll('.gallery-item').forEach(item => {
    item.addEventListener('click', () => {
      console.log('Image clicked - modal can be implemented here');
    });
  });
</script>
